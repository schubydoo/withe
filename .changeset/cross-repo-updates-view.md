---
default: minor
---

Add a cross-repo pending-updates page at /updates. It lists every pending update grouped by dependency, so you see every repository waiting for the same update on one line. Filter the list by update type, including security. The dashboard's pending-updates count now links to it.
