name: Scorecard

# OpenSSF Scorecard — scores the repo's security posture (branch protection,
# pinned deps, token permissions, review) and uploads SARIF to code scanning,
# and publishes to the public OSSF report. Default branch only; the action
# hard-fails on a tag ref, so there is no release trigger.
#
# Uses GITHUB_TOKEN. The Branch-Protection check wants admin:read, which
# GITHUB_TOKEN lacks, so that one check scores lower until a short-lived App
# token is added — everything else scores normally. publish_results needs a
# PUBLIC repo.
#
# Built locally 2026-08-14, not yet pushed.

on:
  branch_protection_rule:
  schedule:
    - cron: "18 7 * * 1"
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  analysis:
    name: OSSF Scorecard
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Run Scorecard
        uses: ossf/scorecard-action@2d1146689b8cda280b9bc96326124645441f03bc # v2.4.4
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true
      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@ff2f1c621b7f889edc0d3c761ac2e6a3f8cdb0dd # v4.37.7
        with:
          sarif_file: results.sarif
          category: scorecard
